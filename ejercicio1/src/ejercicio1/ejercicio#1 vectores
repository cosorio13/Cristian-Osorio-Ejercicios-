/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package ejercicio1;

import java.util.Scanner;
import javax.swing.JOptionPane;

/**
 *
 * @author cristian osorio
 */
public class Ejercicio1 {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        
        int valor= Integer.parseInt(JOptionPane.showInputDialog(null, "Ingrese los datos de la cantidad de servicios a pagar"));// Con este creamos una opcion de resivo de variables que ingrese el usuario en un panel por defecto.
        double [] servicio0 = new double [valor]; // este es el vectr con el indice asignado.
        double [] servicio1 = new double [valor];
        double [] servicio2 = new double [valor];
        double [] servicio3 = new double [valor];
        
        for(int i=0; i<valor; i++){
          double c;
            c = Double.parseDouble(JOptionPane.showInputDialog(null,"Ingrese valor de cada servicio"+(i+1)));
          System.out.println("El valor total es" +i+1);
          
          
        }
    }
    
}
